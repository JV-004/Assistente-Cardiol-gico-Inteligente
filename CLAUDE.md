# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Contexto

Projeto acadêmico da FIAP: **Assistente Cardiológico Inteligente**.

- Remote: `https://github.com/JV-004/Assistente-Cardiol-gico-Inteligente.git`
- Branch principal: `main`
- O nome da pasta local (`Assistente Cardiológico`) contém acentos e espaço; o nome do repo no GitHub foi normalizado para `Assistente-Cardiol-gico-Inteligente`. Sempre cite caminhos locais entre aspas em comandos de shell.

## Estado atual

O repositório está **vazio** — nenhum código, dependência ou configuração foi commitada ainda. As seções abaixo devem ser preenchidas assim que a stack for definida; não presuma linguagem, framework ou layout de pastas antes disso.

## Setup inicial do git (ainda não executado localmente)

```bash
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/JV-004/Assistente-Cardiol-gico-Inteligente.git
git push -u origin main
```

## Comandos

_A definir._ Registrar aqui, quando existirem: instalação de dependências, execução da aplicação, lint, suíte de testes e execução de **um teste isolado**.

## Arquitetura

_A definir._ Documentar aqui apenas o que exige ler vários arquivos para entender — fluxo de dados, limites entre módulos, contratos entre camadas. Não listar arquivo por arquivo.
